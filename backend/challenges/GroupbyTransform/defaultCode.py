import pandas as pd
import numpy as np

df = pd.DataFrame(
            {
                "value": [1, 1, 1, 2, 2, 2, 2],
                "group": ["m", "n", "o", "m", "m", "n", "n"],
            }
        )


def transform(df: pd.DataFrame) -> pd.DataFrame:
    # once you found a solution, define this function
    pass
