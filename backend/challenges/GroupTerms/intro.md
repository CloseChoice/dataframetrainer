For each combination of `id` and `group` values count the number of appearances of `term1`, `term2`, etc.
in the corresponding columns.
Represent the index as a multiindex with in the order: `id`, `group`